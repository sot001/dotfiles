#!/bin/bash
# wrapper to jump via a bastion

HOST=$1

aws ec2 start-instances --instance-id=$(aws --profile readonly ec2 describe-instances --filter "Name=tag:Name,Values=$HOST" --query "Reservations[].Instances[].InstanceId" --output text) 
