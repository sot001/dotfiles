#!/bin/bash
# $@ passes arguments through
TERRAFORM=/usr/local/bin/terraform

if [ ! -z $AWS_PROFILE ]; then
  exec aws-vault exec "${AWS_PROFILE:-dev-readonly}" -- $TERRAFORM "$@"
else
  $TERRAFORM "$@"
fi
